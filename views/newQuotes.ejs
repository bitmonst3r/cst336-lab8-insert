<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lab 8: Add Quote</title>
  <link rel="stylesheet" type="text/css" href="styles.css"/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
  <!-- JavaScript Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
</head>
<body>

  <h1 id="header" class="bg-primary text-white text-center p-5"> Add new Quote </h1>

  <div class="card p-4">
    <div class="card-body text-center">
      <form method="POST" action="/quote/new">
      
        Quote:<br>
        <textarea type="text" name="quote" cols="50" rows="15"></textarea><br><br>

        Category:
        <select name="category">
        <option>Select Category</option>
          <% for (i of categories) { %>
          <option value=<%= i.category %> ><%= i.category %> </option>
          <% } %>
        </select>
        <br><br>

        Likes:
        <input type="text" name="likes"><br><br>

        Author:
        <select name="authorId">
        <option>Select Author</option>
        <% for (i of authors) { %>
          <option value=<%=i.authorId%>><%= i.firstName %> <%= i.lastName %> </option>
        <% } %>
        </select>
        <br><br>
        <button class="btn btn-primary">Add Quote</button>

      </form><br>

      <% if (typeof message != "undefined") { %>
        <h3><%= message %></h3>
      <% } %>
    </div>
  </div>

   
  
</body>
</html>
